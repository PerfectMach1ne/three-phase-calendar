# This is not a bash script that is meant to be runnable, this is just for Neovim highlighting pretties.
# Don't you run this, Luka
curl -v -X POST 127.0.0.1:8057/testTask -d "{ \"json-but\": \"for now it's rly just a string lol\" }" -H "Content-Type: application/json"
